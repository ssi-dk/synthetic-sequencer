# Domain

Read CONTEXT.md and docs/adr/0001-saved-run-replay.md. App code owns replay and
validation. Deployment owns templates, permissions configuration and transport.
Use the repository's terms and report conflicts with its output contract.
